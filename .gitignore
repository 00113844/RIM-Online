/611-RIM-2013-User-guide.pdf
/Entry_Exit_Lock.bas
/Forms_Graphs.bas
/Profile.bas
/Rim_Formulas.md
/RIM_VBA.txt
/zPrint.bas
**/__pycache__/
**/*.pyc
.venv/

/__pycache__

# Data files — not for version control
*.json
*.xlsx
*.xlsm
*.xls
# Generated workbook parameters, captured parity fixtures and the scenario
# definitions that produce them are tracked, despite the blanket *.json above.
!data/*.json
!tests/fixtures/excel_parity/*.json
!scenarios/*.json

# ...except the pre-audit defaults, which are dead code. rim/defaults.py is the
# runtime source until tools/extract_params.py generates data/ from the workbook.
# See .claude/memory/defaults-are-hand-transcribed.md
data/defaults.json

# Agent scaffolding: memory is shared, local settings are not.
.claude/settings.local.json
